name: CI/CD pipeline

on:
    push

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            
            - name: checkout code
              uses: actions/checkout@v4

            - name: Execute the commands
              run: |
                echo "Hello world!"
